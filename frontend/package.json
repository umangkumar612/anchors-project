{
  "name": "forum-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build --prefix frontend",
    "start": "vite preview --host 0.0.0.0 --port $PORT"
  }
}